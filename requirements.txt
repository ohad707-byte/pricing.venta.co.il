# דורש Python 3.10 או חדש יותר.
# חשוב ב-Windows: התקן Python מ-python.org (לא מ-Chocolatey) - מונע בעיות build.
# כל החבילות למטה זמינות כ-wheel מוכן ל-Windows, לא דורשות Visual Studio/קומפילציה.

streamlit>=1.32
pandas>=2.0
openpyxl>=3.1
PyMuPDF>=1.23
Pillow>=10.0
xlsxwriter>=3.1

# חילוץ נתונים מתוכניות PDF מתבצע באמצעות OCR (Tesseract) - דורש גם
# התקנת מערכת בנפרד (לא רק pip):
#   Windows: הורד והתקן מ- https://github.com/UB-Mannheim/tesseract/wiki
#            בזמן ההתקנה סמן את חבילת השפה "Hebrew" ברשימת ה-Additional language data.
#            אם ה-PATH לא מתעדכן אוטומטית, ייתכן שתצטרך להוסיף ידנית את
#            תיקיית ההתקנה (בדרך כלל C:\Program Files\Tesseract-OCR) למשתנה הסביבה PATH.
#   Ubuntu/Debian: sudo apt-get install tesseract-ocr tesseract-ocr-heb
#   macOS:         brew install tesseract tesseract-lang
pytesseract>=0.3.10
